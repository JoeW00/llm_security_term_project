# 第 15 週未完項目執行指南（P1 告警分流）

> **✅ 狀態更新（2026-05-30）：本指南所列項目（§A–§D）已全部於 DGX Spark 執行完成**，真實數字
> 已存檔於 `results/W15-P1-*` 並回填進進度報告。§C 消融並改採全本地四臂（見 `HANDOFF.md`），另加做
> §A.3 類別平衡重訓。以下內容**保留為可重現步驟與方法參考**，不再是待辦。
>
> **⚠️ 個別程式片段為初版示意，最終實作以 repo 內檔案為準**（已更新且與下方片段有出入）：
> - **§A `curate_guide.py`**：最終 `expected.alert_type = IncidentGrade`（TP/BP/FP），**非** `category`；
>   且 `severity` 因 GUIDE 無真值而**不進** `expected`。實際檔：`scripts/data/curate_guide.py`。
> - **§B `Modelfile`**：最終 `FROM ../../out/gguf/triage-f16.gguf` 並**顯式加 Qwen2 ChatML `TEMPLATE` + `stop`**
>   （非下方留空 system 版）。實際檔：`scripts/finetune/Modelfile`。
> - **§C `run_ablation.py`**：最終為**全本地四臂**（規則式 + 同基底 3b + 大模型 32b + 微調），預設免金鑰、
>   雲端臂可選、`severity` 略過。實際檔：`scripts/eval/run_ablation.py`。

> **用途（原始）**：本文件配套《2026-W15-P1-告警分流-個人進度報告.md》。報告初版中標記
> 「⬜ 待補（執行後填入）」的項目，因需 GPU 微調、Ollama 推論或雲端 API 才能產出**真實
> 數字**，當時本機環境無法即時執行而留白。本指南提供**完整、可逐步照做**的方法。
>
> **報告 ⇄ 指南對照**：§A 資料集策展（補報告 §3.2）、§B LoRA 微調（補報告 §5.2）、
> §C 消融評估（補報告 §6.3）、§D 注入量測（補報告 §7.2）。
>
> **執行順序**：A → B → C 為依賴鏈；D 可獨立並行。
>
> **不變的契約**：所有步驟都**不更動** `soc_agent/state.py`、`IncidentState` 或既有
> 圖結構；微調權重透過 `OllamaClassifier(model=...)` **零下游改動**接入。新增的都是
> `scripts/` 與 `data/` 下的離線資產，**絕不進 pytest**（維持測試離線、確定性）。

---

## 前置環境

```bash
# 專案根目錄
cd "term_project"
uv sync                          # 同步既有依賴
uv run pytest -q                 # 確認基線：應 124 passed

# 微調 / 推論額外依賴（建議獨立 venv 或 extra group，避免污染主依賴）
#   - GPU 機器：unsloth, transformers, peft, trl, datasets, bitsandbytes
#   - 本地服務：ollama（https://ollama.com 安裝後 `ollama serve`）
#   - 雲端基線：anthropic（專案已有 llm extra group）
```

> **建議**：把微調相依放進獨立的 `uv` extra group（如 `--group finetune`），或在
> 一台有 GPU 的機器／Colab 上跑訓練，產出 GGUF 後再回到本機 `ollama create`。

---

## §A 資料集策展 — 補報告 §3.2

**目標**：下載 Microsoft GUIDE 資料集，對映成我們凍結的 JSONL 留出集格式，產出
`data/triage/train.jsonl` 與 `data/triage/holdout.jsonl`，並輸出統計摘要。

### A.1 取得資料集

GUIDE / Security Incident Prediction 公開於 Kaggle / Microsoft。下載後置於
`data/triage/raw/`（此目錄已在 `.gitignore` 忽略大型檔，勿提交原始資料）。

```bash
mkdir -p data/triage/raw
# 依資料源說明下載（Kaggle CLI 或網頁），放到 data/triage/raw/
```

### A.2 策展腳本 `scripts/data/curate_guide.py`

```python
"""把 GUIDE 原始資料對映成凍結的 JSONL 留出集格式並切分 train/holdout。

輸出：data/triage/train.jsonl, data/triage/holdout.jsonl, data/triage/stats.json
本腳本為離線資料前處理，絕不進 pytest。
"""
from __future__ import annotations

import json
import random
from collections import Counter
from pathlib import Path

RAW = Path("data/triage/raw")
OUT = Path("data/triage")
SEVERITY = {"low", "medium", "high", "critical"}


def normalize_severity(v: str) -> str:
    v = (v or "").strip().lower()
    # 視 GUIDE 實際取值調整對映；非四級者保守歸 medium
    return v if v in SEVERITY else "medium"


def to_record(row: dict) -> dict | None:
    """GUIDE 一列 → {'alert': {...}, 'expected': {...}}；無法對映則回 None。"""
    category = (row.get("Category") or row.get("category") or "").strip().lower()
    if not category:
        return None
    indicators = [x for x in (row.get("entities") or []) if x]
    alert = {
        "source": "guide",
        "timestamp": row.get("Timestamp", "t"),
        "category": category,
        "severity": normalize_severity(row.get("Severity", "")),
        "message": (row.get("AlertTitle") or row.get("title") or "").strip(),
        "indicators": indicators,
        "raw": {},
    }
    expected = {"alert_type": category, "severity": alert["severity"]}
    return {"alert": alert, "expected": expected}


def main() -> None:
    rows = []  # ← 依實際檔案格式讀入 RAW 下的 CSV/JSON
    # for src in RAW.glob("*.csv"): rows.extend(csv.DictReader(...))
    records = [r for row in rows if (r := to_record(row))]
    random.Random(42).shuffle(records)
    split = int(len(records) * 0.8)
    train, holdout = records[:split], records[split:]

    for name, recs in (("train", train), ("holdout", holdout)):
        with open(OUT / f"{name}.jsonl", "w", encoding="utf-8") as f:
            for r in recs:
                f.write(json.dumps(r, ensure_ascii=False) + "\n")

    stats = {
        "raw_rows": len(rows),
        "kept": len(records),
        "train": len(train),
        "holdout": len(holdout),
        "alert_type_dist": dict(Counter(r["expected"]["alert_type"] for r in records)),
        "severity_dist": dict(Counter(r["expected"]["severity"] for r in records)),
    }
    (OUT / "stats.json").write_text(json.dumps(stats, ensure_ascii=False, indent=2))
    print(json.dumps(stats, ensure_ascii=False, indent=2))


if __name__ == "__main__":
    main()
```

```bash
uv run python scripts/data/curate_guide.py    # 產出 train.jsonl / holdout.jsonl / stats.json
```

### A.3 清理規則（在 `to_record` / `main` 內實作）

- **去重**：以 `(category, message)` 為鍵去重複列。
- **空值**：`category` 或 `message` 為空者丟棄。
- **嚴重度正規化**：非四級者保守歸 `medium`（見 `normalize_severity`）。
- **類別不平衡**：若某類過少，於 `stats.json` 標註，必要時對訓練集（非留出集）重採樣。

### ✅ 回填報告

把 `stats.json` 的數字填入報告 §3.2 的「正式 GUIDE 資料集統計」表。

---

## §B LoRA 微調 — 補報告 §5.2

**目標**：用 §A 的 `train.jsonl` 微調本地小模型，匯出 GGUF，`ollama create` 成可被
`OllamaClassifier` 呼叫的模型。

### B.1 生成訓練樣本 `scripts/finetune/build_dataset.py`

關鍵：**prompt 模板完全沿用** `soc_agent/classifiers/prompts.py`，使訓練格式＝推論格式。

```python
"""train.jsonl → 微調用 {prompt, completion} 樣本（沿用 prompts.py 模板）。"""
from __future__ import annotations

import json
from pathlib import Path

from soc_agent.classifiers.prompts import TRIAGE_SYSTEM_PROMPT, build_triage_prompt

src = Path("data/triage/train.jsonl")
dst = Path("data/triage/sft.jsonl")

with open(src, encoding="utf-8") as f, open(dst, "w", encoding="utf-8") as out:
    for line in f:
        rec = json.loads(line)
        completion = json.dumps(
            {
                "alert_type": rec["expected"]["alert_type"],
                "severity": rec["expected"]["severity"],
                "confidence": 1.0,
            },
            ensure_ascii=False,
        )
        out.write(json.dumps({
            "system": TRIAGE_SYSTEM_PROMPT,
            "prompt": build_triage_prompt(rec["alert"]),
            "completion": completion,
        }, ensure_ascii=False) + "\n")
```

### B.2 微調 `scripts/finetune/train_lora.py`（Unsloth + LoRA，在 GPU 機器執行）

```python
"""Unsloth + LoRA 微調分流模型。需 GPU。"""
from unsloth import FastLanguageModel
from trl import SFTTrainer
from transformers import TrainingArguments
from datasets import load_dataset

BASE = "unsloth/Llama-3.2-3B-Instruct"   # 或 Qwen2.5-3B 等小模型
model, tok = FastLanguageModel.from_pretrained(BASE, max_seq_length=1024, load_in_4bit=True)
model = FastLanguageModel.get_peft_model(
    model, r=16, lora_alpha=32, lora_dropout=0.05,
    target_modules=["q_proj","k_proj","v_proj","o_proj","gate_proj","up_proj","down_proj"],
)

ds = load_dataset("json", data_files="data/triage/sft.jsonl", split="train")
def fmt(ex):
    return {"text": f"<|system|>\n{ex['system']}\n<|user|>\n{ex['prompt']}\n<|assistant|>\n{ex['completion']}"}
ds = ds.map(fmt)

trainer = SFTTrainer(
    model=model, tokenizer=tok, train_dataset=ds, dataset_text_field="text",
    max_seq_length=1024,
    args=TrainingArguments(
        per_device_train_batch_size=2, gradient_accumulation_steps=4,
        learning_rate=2e-4, num_train_epochs=3, logging_steps=10,
        output_dir="out/lora-triage", report_to="none", seed=42,
    ),
)
trainer.train()                              # ← 記錄 train/eval loss
model.save_pretrained("out/lora-triage")
```

### B.3 匯出 GGUF + `ollama create`

```bash
# Unsloth 可一步匯出合併後 GGUF：
#   model.save_pretrained_gguf("out/gguf-triage", tok, quantization_method="q4_k_m")
# 或用 llama.cpp 的 convert + quantize。

cat > scripts/finetune/Modelfile <<'EOF'
FROM ./out/gguf-triage/model.gguf
# system prompt 留空：分流的 system prompt 由程式（prompts.TRIAGE_SYSTEM_PROMPT）注入
PARAMETER temperature 0
EOF

ollama create soc-triage -f scripts/finetune/Modelfile
ollama run soc-triage "test"                 # 確認模型可載入
```

### ✅ 回填報告

把基礎模型、樣本數、epochs、訓練時間、最終 loss、GGUF 大小、ollama 模型名填入報告 §5.2。

---

## §C 消融評估 — 補報告 §6.3（核心數字）

**目標**：把微調模型接上 `OllamaClassifier`，與雲端零樣本、規則式並列跑 `ablation`，
產出 `alert_type` / `severity` 的 Accuracy / macro-F1 表與混淆矩陣。

### C.1 Runner `scripts/eval/run_ablation.py`（手動腳本，不進 pytest）

```python
"""微調本地 vs 雲端零樣本 vs 規則式 的消融評估。需 ollama serve（+ 雲端金鑰）。"""
from __future__ import annotations

import json
from typing import Any

import ollama  # 官方 client

from soc_agent.classifier import RuleBasedClassifier
from soc_agent.classifiers.ollama import OllamaClassifier
from eval.triage_eval import ablation, load_dataset
from eval.zero_shot import ZeroShotClassifier


class OllamaAdapter:
    """把官方 ollama client 包成 OllamaClient Protocol（generate→{'response':...}）。"""
    def generate(self, *, model: str, system: str, prompt: str) -> dict[str, Any]:
        r = ollama.generate(model=model, system=system, prompt=prompt, options={"temperature": 0})
        return {"response": r["response"]}


class AnthropicChat:
    """把 anthropic client 包成 ChatClient Protocol（complete→text）。供雲端零樣本臂。"""
    def __init__(self, client, model: str): self._c, self._m = client, model
    def complete(self, *, system: str, prompt: str) -> str:
        msg = self._c.messages.create(
            model=self._m, max_tokens=256, system=system,
            messages=[{"role": "user", "content": prompt}],
        )
        return msg.content[0].text


def main() -> None:
    records = load_dataset("data/triage/holdout.jsonl")
    import anthropic
    classifiers = {
        "rule_based": RuleBasedClassifier(),
        "zero_shot_cloud": ZeroShotClassifier(AnthropicChat(anthropic.Anthropic(), "claude-haiku-4-5-20251001")),
        "finetuned_local": OllamaClassifier(OllamaAdapter(), model="soc-triage"),
    }
    for target in ("alert_type", "severity"):
        print(f"\n=== {target} ===")
        for name, m in ablation(classifiers, records, target).items():
            print(f"{name:18s} acc={m.accuracy:.3f} macroF1={m.macro_f1:.3f}")
            print("  per_class_f1:", json.dumps(m.per_class_f1, ensure_ascii=False))
            print("  confusion:", json.dumps(m.confusion, ensure_ascii=False))


if __name__ == "__main__":
    main()
```

```bash
ollama serve &                               # 背景啟動本地服務
export ANTHROPIC_API_KEY=...                 # 雲端零樣本臂
uv run python scripts/eval/run_ablation.py | tee out/ablation_results.txt
```

### ✅ 回填報告

把三臂的 Accuracy / macro-F1 填入報告 §6.3 的兩張表；附微調本地模型的混淆矩陣與逐類 F1。
**分析要點**：微調本地 vs 雲端零樣本的差距，即「微調對窄任務分類的實質貢獻」，是本子系統
的核心結論。

---

## §D 提示注入量測 — 補報告 §7.2（防禦前後）

**目標**：量化「本子系統防禦」對提示注入的效益——以相同對抗集，比較「關閉防禦」與
「開啟防禦」兩種設定的 `manipulation_rate`。

### D.1 擴充對抗集

在 `eval/injection_eval.py` 的 `INJECTION_PAYLOADS` 基礎上，新增針對分流的 payload
（指令注入、角色扮演、假 `<<<END ALERT>>>` 標記、假 system 行等），並涵蓋
`message` 與 `indicators` 兩個欄位（`inject(..., field_name="indicators")`）。

### D.2 防禦開關對照

```python
"""分流層注入量測：同一對抗集，比較防禦關閉/開啟下的被操控率。手動腳本。"""
from eval.injection_eval import default_corpus, run_injection_suite
# 防禦關閉：自寫一個 naive runner——直接把 message 當指令拼進 prompt、輸出不驗證。
# 防禦開啟：用本子系統 pipeline（build_triage_prompt 分隔 + parse_classification 驗證 + 退路）。
# 兩者各跑 run_injection_suite，比較 report.manipulation_rate。
```

> 量測「被操控」的判準：注入後分類結果是否偏離 benign 期望（如被誘導改變 `severity`
> 或 `alert_type`）。防禦開啟臂應因「分隔 + 常數 system + 驗證 + 退回規則式」而顯著降低
> 被操控率。

### ✅ 回填報告

把兩種設定的 `對抗案例數 / 被操控數 / manipulation_rate` 填入報告 §7.2 的表，量化防禦效益。

---

## 完成檢查清單

- [x] §A：`train.jsonl` / `holdout.jsonl` / `stats.json` 產出 → 回填報告 §3.2（`results/W15-P1-A-*`）
- [x] §B：`soc-triage` ollama 模型建立、訓練曲線記錄 → 回填報告 §5.2（`results/W15-P1-B-finetune.md`）
- [x] §C：完整四臂消融產出 → 回填報告 §6.3（含混淆矩陣；`results/W15-P1-C-full-ablation.*`）
- [x] §D：注入防禦前後 `manipulation_rate` → 回填報告 §7.2（`results/W15-P1-D-injection.*`）
- [x] §A.3 加分：類別平衡重訓 → §6.4（`results/W15-P1-B2-balanced-retrain.*`）
- [x] 全程 `uv run pytest -q` 仍 124 passed（手動腳本不進測試套件）
- [x] 大型原始資料與權重**不提交**（`.gitignore` 已忽略 `data/triage/raw/`、`out/`）
